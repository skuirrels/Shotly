{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "scroll",
  "description": "Capabilities for the scrolling-capture window: region selection first, then the floating HUD. It drags itself around and otherwise only invokes app commands.",
  "windows": ["scroll"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-set-focus",
    "core:window:allow-start-dragging"
  ]
}
